// Package config bassit/internal/config/input.go
package config

var (
    // ShiftedKeyEnabled indicates whether the shifted keys trigger the same action as the unshifted keys
    ShiftedKeyEnabled = true
)
