# test

Some functions that use
`bubbletea` to draw content on the terminal are not easy to write test cases for verification, so this directory contains some manual tests and checks.

You can run specific tests by running the following command:

```bash
go run ./test/${test_name}
```
